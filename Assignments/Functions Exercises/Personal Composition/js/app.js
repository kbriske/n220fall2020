let moonX = 0;
let moonY = 200;

function setup() {
    createCanvas(700,400);
    background(0);
}

function draw() {
    background(0);
    moonX++;
    moonY++;
}

function drawMoon(moonColor,crater,heightX,heightY) {
    console.log(moonColor);
}